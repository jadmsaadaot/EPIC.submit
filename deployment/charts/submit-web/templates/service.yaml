apiVersion: v1
kind: Service
metadata:
  annotations: null
  creationTimestamp: null
  labels:
    app: {{ .Release.Name }}
  name: {{ .Release.Name }}
spec:
  ports:
  - name: "8080"
    port: {{ .Values.service.port }}
    targetPort: 8080
  selector:
    app: {{ .Release.Name }}
status:
  loadBalancer: {}
