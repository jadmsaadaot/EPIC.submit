apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "submit-api-bc.fullname" . }}-test-connection"
  labels:
    {{- include "submit-api-bc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "submit-api-bc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
